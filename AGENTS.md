# Hello Stay

Hello Stay 民宿官方網站，使用 Next.js 建構。

## 工作規則

1. 開始工作前先讀 `context.md` 了解專案狀態
2. 不要直接修改 `.env` 或 `next.config.js`
3. 完成工作後更新 `context.md` 的進度與變更記錄

## 進度管理

- 重要的架構決策記錄在 `context.md`
- 每次工作結束時更新 context 中的「最近變更」區塊
