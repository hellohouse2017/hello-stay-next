# Hello Stay

## 簡述
Hello Stay 民宿的官方網站前台。

## Tech Stack
- **Framework**: Next.js
- **Frontend**: React, TypeScript
- **Styling**: TailwindCSS
- **Version Control**: Git

## 最近變更
- 2026-08-23（住客評價頁面圖片精準配對與部署）：針對 `/reviews` 進行全面視覺素材調優。升級四大空間焦點實景配圖（中島廚房特寫、1F 挑高客廳麻將電視全景、4F 落地採光六人房與獨立衛浴套房、鹽埕大港橋旋轉橋港灣美景）；並在 8 組真實住客評價卡片中，全面嵌入對應之真實場景實景縮圖（如迎娶採光新娘房、中島備餐火鍋、4F 獨立交誼長桌等）；頂部 Hero 增設三大經典空間氛圍縮圖列。驗證：`npm run validate:content`、`npx eslint`、`npm run build:local`（159 routes）全數通過，並執行 Vercel production 部署。
- 2026-08-23（住客評價頁面翻新與部署）：徹底翻新 `/reviews` 頁面，消除 AI 生硬說教語氣，重寫為涵蓋 18人家族、好友中島火鍋、高雄迎娶、雙家庭獨棟、學生畢旅與公司團建等真實場景之口碑內容。UI/UX 升級為 Google 商家 4.9★ 官方認證看板、四大體驗維度評分條（5.0/4.9/4.9/4.8）、多維度情境與館別篩選器（Filter Tabs）、精緻驗證住客卡片（Verified Review Cards）、四大空間亮點實景對照（Photo Spotlight Gallery）與零廢話真誠 FAQ。SEO 全面覆蓋「高雄包棟民宿評價/推薦」、「中島廚房包棟」、「麻將包棟」等高意圖詞，並升級 LodgingBusiness/AggregateRating/Review/FAQPage 結構化資料。驗證：`npm run validate:content`、`npx eslint`、`npm run build:local`（159 routes）全數通過，並執行 Vercel production 部署。
- 2026-08-23（交通頁實景圖替換並部署）：完成 `/traffic` 頁面全數圖片替換，將 Hero 背景替換為高雄輕軌穿行駁二與高流實景圖（`hero.webp`）、四種交通方式卡片分別配置真實實景圖（捷運鹽埕埔站 2 號出口 `mrt.webp`、高鐵左營站列車月台 `hsr.webp`、鹽埕立體停車場 `parking.webp`、計程車/叫車下車點 `dropoff.webp`），共用地圖替換為無假電話之正式交通指引圖（`guide.webp`），並在 `SOURCES.md` 記錄創用 CC 授權來源。同步更新探索首頁交通導覽卡片配圖。驗證：`npm run validate:content`、`npm run lint`、`npm run build:local`（159 routes）全數通過。
- 2026-08-23（交通圖替換並部署）：將使用者提供的新版 Hello Stay／Guesthouse 交通指引圖覆蓋至 `public/images/traffic/guide.png`，並連同前一輪三篇旅宿攻略文章封面替換一併執行 Vercel production deploy。Deployment `hello-stay-next-47uutns9s-hellohouses-projects.vercel.app` 已完成並 alias 至 `https://www.hello-stay.com`。Vercel build 通過公開內容驗證、TypeScript 與 159 routes；正式站 `/traffic`、`/blog`、`/explore` 均 200，新圖片遠端 SHA-256 與本機素材一致，三篇文章封面 marker 亦確認仍在線上。此次部署由使用者明確授權；未修改環境檔、未修改 `booking-core`／`line-bot-v2`、未執行外部資料寫入。
- 2026-08-23：依使用者提供的新版交通指引圖替換 `public/images/traffic/guide.png`。新版為 Hello Stay／Guesthouse 品牌版導航地圖，會同步套用至 `/traffic` 與探索頁使用該素材的區塊；與前一輪旅宿攻略三篇文章封面替換一併準備部署。驗證：`npm run validate:content`、`npm run lint`（0 errors、保留 1 個既有 `indexnow` unused warning）、`npm run build:local`（159 routes）、`git diff --check` 全部通過。
- 2026-08-23：替換旅宿攻略頁「國旅補助／交通指南」文章卡片的重複地圖封面。`src/app/blog/page.tsx` 改為依文章主題使用既有實景素材：外國旅客文章使用高雄港景、國旅補助指南使用多人共餐、透明定價指南使用你好哇寓所中島廚房；交通頁原本的地圖素材未修改。驗證：`npm run validate:content`、`npx eslint src/app/blog/page.tsx`、`npm run build:local`、`git diff --check` 全部通過。
- 2026-08-23（部署與正式站驗證）：將本輪 SEO 導流與手機 CTA 變更部署至 Vercel production，deployment `hello-stay-next-fqn7akkc2-hellohouses-projects.vercel.app`，已 alias 至 `https://www.hello-stay.com`。Vercel 遠端 `npm run build` 通過公開內容驗證、TypeScript 與 159 routes 建置。正式站驗證：`/kaohsiung-whole-house`、`/compare`、`/explore/food`、`/sitemap.xml`、`/robots.txt` 均 200；`/features/kitchen`、`/features/parking`、舊演唱會 URL 均正確 308 redirect；10 人文章手機固定 CTA 可見並實際點擊至 `/book?guestCount=10`；美食頁中段住宿橋接可見，訂房 CTA 帶 `data-cta-position=mid_content`。Production env 已確認存在 `BOOKING_CORE_INTERNAL_URL`、`SEO_FUNNEL_SHARED_SECRET`、GA4 OAuth 與 MongoDB 相關設定；未讀取或暴露密鑰。未修改 `booking-core`、`line-bot-v2` 或外部資料，未要求 GSC 重新檢索。未授權直接呼叫 `/api/cron/seo-health` 回 401，符合正式環境保護規則。
- 2026-08-23（部署前 SEO 計畫對照補強，未部署）：再次逐項核對貼上的 90 天計畫後，確認 `/compare` 保留比較／評比意圖是正確分工，未做無差別改鏈；補上高流量內容頁手機固定 CTA（文章頁與 `/explore/food`／`/explore/spots` 共用閱讀元件，手機底部提供「查空房與報價」及「LINE 諮詢」，人數文章保留 `guestCount`、`seo_intent`、`party_size`、`cta_position=sticky_mobile`）；`/explore/food` 在第一組餐飲清單後新增住宿橋接卡，連往 `/kaohsiung-whole-house` 與 `/book`，並補 content bridge / CTA 追蹤欄位。`scripts/verify-local-seo-crawl.ts` 新增上述兩項輸出護欄。驗證：目標 ESLint、`npm run lint`（0 errors、1 個既有 `indexnow` unused warning）、`npx tsc --noEmit --pretty false`、`npm run validate:content`、`npm run test:seo-ranking-trends`、`npm run test:seo-local-crawl`（121 pages／0 critical／0 warning／2 opportunity）、`npm run build:local`（159 routes）、`git diff --check` 全部通過。部署前仍需由維運在正式環境確認 `SEO_FUNNEL_SHARED_SECRET` 與 `BOOKING_CORE_INTERNAL_URL`，本輪未修改環境檔、未部署、未要求 GSC 重新檢索。
- 2026-08-23: 依最新 SEO 導流投資順序完成第二批內容與訂房轉換實作（未部署）。排程文章 `kaohsiung-10-person-stay`、`kaohsiung-20-person-stay`、`kaohsiung-30-person-stay` 與 `kaohsiung-family-accommodation` 更新 title、description、首段直答、FAQ、樓梯／分房／容量等現況資訊與 `dateModified`；MDX 文章 `kaohsiung-kitchen-bnb`、`kaohsiung-mahjong-stay` 補正式開伙／簡易備餐、設備位置、23:00 安靜規範與人數導向 CTA。共用動態文章頁新增 6／10／15／20／30 人 `guestCount` 映射，文章底部與 Content Bridge 會把人數帶入 `/book?guestCount=`；`PropertyLinksBlock` 的 10／20／30 人連結改回對應人數攻略，`/book` CTA 補上 `seo_intent`、`party_size`、`cta_position` 追蹤欄位，AnalyticsTracker 同步支援 15 人意圖。驗證：`npm run validate:content`、`npx tsc --noEmit --pretty false`、`npm run lint`（0 errors、保留 2 個既有 warnings）、目標 ESLint、`npm run test:seo-ranking-trends`、`npm run test:seo-route-boundaries`、`npm run test:seo-page-health`、`npm run test:seo-local-crawl`（121 pages／0 critical／0 warning／2 opportunity）、`npm run build:local`（159 routes）、`git diff --check` 全部通過；未部署、未要求 GSC 重新檢索、未修改環境檔或外部平台。`test:seo-local-crawl` 第一次因本機未啟動 sitemap 服務失敗，啟動 `127.0.0.1:3012` 後重跑通過。
- 2026-08-21: 強化國旅補助試算規則與免責條款。全面同步並落實「國旅補助僅適用於平日（週日至週四）入住，週五、週六（週末假日）、國定假日與連續假期全數不適用補助折扣」之業務規範，避免試算誤算產生糾紛。更新首頁國旅補助免責聲明、兩篇國旅補助專題文章（taiwan-travel-subsidy-pricing-guide.mdx、taiwan-travel-subsidy-guide.mdx）之試算情境警語與權益聲明條款，並同步在 LINE Bot 知識庫（curated-v2-knowledge-data.mjs）新增國旅補助平日限定問答條目。驗證：主站 `npm run validate:content`、`npm run lint`、`npm run build:local`（159 routes）、LINE Bot `npm run check`、`node --test tests/knowledge*.test.js` 全數通過。
- 2026-08-21: 完成全站 Google SEO 高價值語意內鏈（Contextual Authority）強化工程。全面涵蓋 2 篇高流量 2026 國旅補助專題長文、21 篇主題情境/景點/生活美食 MDX 文章、核心路由頁面（包棟總覽、關於我們、交通與停車、首頁模板與 PropertyLinksBlock 元件）。建立精準語意映射字典（溝頂民宿 ➔ `/godin`、你好哇寓所 ➔ `/hellohouse`、雙館包棟 ➔ `/compare`、高雄包棟全攻略 ➔ `/kaohsiung-whole-house`、即時查房 ➔ `/book`、交通指南 ➔ `/traffic`、駁二/大港橋/中島廚房/麻將等情境關鍵字首次提及自然鏈接）。修復 PropertyLinksBlock 中 key 重複問題，首頁雙館包棟卡片房型細節直連 `/compare`。驗證：`npm run validate:content`、`npm run lint`、`npx tsc --noEmit`、`npm run build:local`（159 靜態路由全數通過）、`npm run test:seo-route-boundaries` 與 `npm run test:seo-page-health` 全數通過。
- 2026-08-21: 完成國旅補助防坑與透明定價專題文章上架、首頁方案卡片引流標籤、試算情境館別直連導流與免責聲明強化。文章試算情境全面建立直連館別頁面內鏈（情境 A 直連 `/godin`、情境 B 直連 `/hellohouse`、情境 C 直連 `/compare`），首頁方案卡片新增補助標籤（溝頂最高折 $8,000、你好哇平日連住 5 房最高折 $10,000、雙館最高折 $18,000），卡片與文章均明確標註國旅補助預訂須知與權益免責聲明。更新 `src/content/articles/taiwan-travel-subsidy-pricing-guide.mdx`、`src/content/articles/taiwan-travel-subsidy-guide.mdx`、`src/components/HomeTemplateExperience.tsx` 與 `src/styles/luxury.css`。驗證：`npm run validate:content`、`npm run lint`、`npm run build:local`（159 routes）全數通過。
- 2026-08-18: 完成 Hello Stay 官網高級質感 UI/UX 重新規劃與實作。視覺升級為「Quiet Luxury（靜奢度假感）+ 雜誌編輯風（Editorial Hospitality）」設計語言，採用頂級燕麥米白（`#f7f5ef`）、炭木墨黑（`#181b19`）、雅緻黃銅金（`#b38547`）與深綠（`#184e41`），升級卡片瀰散光影與微光邊框。首頁新增人數篩選 Segment Control（全部方案、4-12 人溝頂整棟、13-26 人你好哇中島廚房、27-36 人雙館合住），即時動態過濾並高亮推薦方案；館別展示卡片（Villa Showcase Cards）新增特色 Badge、圖標規格膠囊、起始價格與雙 CTA 按鈕（看空間介紹 / 查空房直訂）；強化 Hero 預訂搜尋艙（Booking Pod）與手機端響應式排版。保留所有真實外部 booking query、日期驗證、超出 36 人自動提示轉 LINE、SEO/JSON-LD/GSC/Canonical 設定。驗證：`npm run validate:content`、`npm run lint`、`tsc --noEmit`、`npm run build:local`（156 routes）、`npm run test:seo-route-boundaries`、`npm run test:seo-page-health` 全部通過。未修改 `booking-core`、未更動 `.env` 或 `next.config.ts`。
- 2026-08-16: 完成 Hello Stay SEO / AEO 雙軌增長計畫第一輪本機實作，未部署。首頁、`/compare`、`/kaohsiung-whole-house`、`/explore/food`、世運／巨蛋文章與 10 人文章完成搜尋意圖、title/H1、直答、FAQ、schema 與角色化內鏈調整；比較頁只列目前可訂的你好哇、溝頂與雙館，規劃中的大智若愚移出主要 ItemList。美食頁以資料自動呈現 20 間餐飲，補分類導覽、Breadcrumb、ItemList、可見 FAQ 與 content bridge；巨蛋頁補左營／鹽埕的誠實決策說明；三天兩夜文章保留原 title 並補人數橋接。同步清理公開文章過期容量、價格、折扣、固定評價與年資敘述，未來排程只保留 `kaohsiung-legal-bnb`、`kaohsiung-music-center-stay`、`yancheng-walkable-stay` 三篇；其餘改 draft。新增 `SeoLandingConversion`、`SeoQueryPageOpportunity`、GA4 `content_bridge_click`、SEO health 向後相容欄位、站外權威稽核文件 `docs/seo-offsite-authority-audit-2026-08-16.md`，並修正民宿合法登記號與 About 的可訂館別 schema。移除 7 個與 MDX 重複的舊靜態文章 route，新增公開內容重複來源護欄與 sitemap/FAQ/ItemList 本機爬檢。`seo:analyze` 已能載入本機 Next env 並讀取 2026-08-08 snapshot，只輸出優先建議。
- 2026-08-16: 收尾 GA4 初始化可靠性：`src/app/layout.tsx` 的初始化腳本改為 `beforeInteractive`，`src/components/AnalyticsTracker.tsx` 增加具 config guard 與 AI referrer allowlist 的 client fallback queue，避免 inline script 未執行時遺失事件或 AI sessions。已透過頁面主執行環境確認 `gtag`、`dataLayer`、`config G-LKVWPNVH5M` 與 `/explore/food` → `/compare` 的 `content_bridge_click` 均正常。驗證通過：`npm run validate:content`、`npm run lint`、`npx eslint src/app/layout.tsx src/components/AnalyticsTracker.tsx`、`npx tsc --noEmit --pretty false`、`npm run build:local`（156 routes）、`npm run test:seo-ga4`、`test:seo-page-health`、`test:seo-ranking-trends`、`test:seo-gsc-runtime`、`test:seo-report-formatters`、`test:seo-health-route-payload`、`npm run test:seo-local-crawl`（118 頁／0 critical／0 warning／0 opportunity）與 `git diff --check`。未部署、未要求重新索引、未修改外部平台或環境檔。
- 2026-08-13: 延續 `/explore` 素材語意核對，將「鹽埕必吃美食」主題卡改用實際芒果冰照片，將「老屋咖啡香」改用新濱·駅前冰滴咖啡設備照片，並同步修正文案與 alt，避免以市場入口或泛老屋室內照代替主題內容。素材仍由 `public/images/explore` 的 Wikimedia Commons 授權檔案提供；production deployment `hello-stay-next-3l7k42isn-hellohouses-projects.vercel.app` 已 alias 至 `https://www.hello-stay.com`。本機與正式站 smoke test 均通過，未修改 `booking-core`、`line-bot-v2` 或環境檔。
- 2026-08-13: 完成 `/explore` 鹽埕在地探索頁精品旅宿 UI 重構並部署。保留 `local-guides.ts`、交通頁、停車資料與既有文章資料來源，改為滿版 Hero、分類導覽、四張 editorial 主題卡、人氣推薦橫向列、住宿 CTA 與延伸資訊入口；沿用 `public/images` 真實住宿/街區圖片，未新增假 stock image。`LocalGuidePage` 補齊 food/spots 分段 anchor，`Navbar` 補 `/explore` active 狀態與 underline，OG 圖片改與可見 Hero 一致。保留既有美食/景點/交通/住宿/地圖連結與 mobile menu；desktop 1440×1000、tablet 820px、mobile 390×844 實測無水平溢位，分類切換、人氣推薦左右控制與 mobile menu 可操作。`npm run validate:content`、`npm run lint`、`npx tsc --noEmit`、`npm run build:local`（156 routes）與 `git diff --check` 通過；Production deployment `hello-stay-next-fkqmi922k-hellohouses-projects.vercel.app` 已 alias 至 `https://www.hello-stay.com`，正式 `/explore`、`/explore/food`、`/explore/spots`、`/traffic` 均回 200。本次只修改 `主站官網`，未修改 `booking-core`、`line-bot-v2` 或環境檔。
- 2026-08-12: 將首頁精品旅宿 UI 最終版部署至 Vercel production。Deployment `hello-stay-next-ac7zjfca4-hellohouses-projects.vercel.app` 已 alias 至 `https://www.hello-stay.com`；Vercel 遠端 `npm run build` 通過內容驗證、TypeScript 與 156 routes 建置。上線後首頁、`/book`、`/compare`、`/blog/yancheng-night-guide`、`/sitemap.xml` smoke test 正常，首頁已確認新版 Hero 與查房列內容；未修改 `booking-core`，未執行外部訂房或資料寫入。
- 2026-08-12: 完成 Hello Stay 首頁精品旅宿 UI 重構。以既有 `properties.ts`、`public-stay-facts.ts`、`local-guides.ts` 與 `homepage-faq.ts` 為資料來源，重做滿版實景 hero、懸浮日期/人數查房列、信任訊息、依人數選住宿、房源比較表、真實旅客評價、鹽埕 editorial 指南、FAQ 與 final CTA；保留外部 booking query、日期驗證、人數推薦、超出範圍轉 LINE、輪播、mobile menu 與既有內頁連結。更新 `HomeTemplateExperience.tsx`、`Navbar.tsx` 與 scoped `src/styles/luxury.css`，未修改 `booking-core`。查房列調整為僅跨越 Hero 約 24px，避免遮住輪播主視覺；移除與正式 MDX 重複的 scheduled `yancheng-night-guide` 來源，保留 MDX 作為唯一公開來源。`npm run validate:content`、`npm run build:local`（156 routes）、`npx tsc --noEmit`、`npm run lint`、`npx next build --webpack` 與 `git diff --check` 通過。桌面 1440×1000、手機 390×844 實際檢查無水平溢位、僅一個 `<main>`，mobile menu、FAQ accordion、hero controls 與 booking 人數推薦可操作。
- 2026-08-04: 修正公開內容來源衝突：`dagangqiao-attraction-guide` 已有正式 MDX 版本，因此將同 slug 的 scheduled entry 標記為 draft，保留單一公開來源。`validate:content` 與 `build:local` 已通過。
- 2026-08-04: 依 UI/UX 稽核結果修正訂房與館別導覽。Booking Core 無完整日期進入 `/booking` 時先停在日期選擇，不再直接跳結果；手機結果狀態隱藏會遮住卡片的固定底部列；日曆價格改為 `NT$` 完整格式並補月曆／床位圖示按鈕 aria-label；簽約、付款缺驗證資訊時補訂單查詢、回訂房與 LINE 客服恢復入口；`/order/list` 無 items 改導訂單查詢。主站 8-12 人改為兩館比較推薦，`/book` 通用 CTA 不再預選你好哇並帶入日期選擇；你好哇寓所、溝頂民宿長頁新增 sticky 段落導覽；訂房入口桌機 hero 文案改為垂直置中。Booking Core build、兩專案 targeted eslint 與 typecheck 通過；主站 `build:local` 目前被既有 `dagangqiao-attraction-guide` MDX/scheduled duplicate content gate 擋住。
- 2026-07-27 (第三批・日韓市場準備): 完成日韓旅客開放前的官網補強並部署。修復 ja/ko/en/vi 首頁 title 雙品牌尾綴（根因：locale layout 的 title.template 只作用於子區段，同區段 page 吃到根 layout 模板；改用 `title:{absolute}` 修復）。ja/ko 首頁新增可見 FAQ（各 6 題）＋FAQPage JSON-LD，資料層為新檔 `src/i18n/home-faq.ts`（事實同步自 public-stay-facts/llms-faq，含溝頂無電梯說明）。新增 `llms-ko.txt`（`buildKoLlmsText`＋`llmsFaqKo` 6 題）與 head 的 ko alternate link。新增 `/ja/agreement` 與 `/ko/agreement` 入住須知頁（共用 `AgreementPageContent`，逐條對照 zh 版：16:00/11:00、遲退 NT$1,800/hr 逾 4hr 續住一天、全額預收、保留一年折抵、超額 NT$1,800/人晚、事先加人平日 1,000/假日 1,500、禁菸、安寧 23-08、寵物書面同意 NT$800、合法收據不開統一發票），hreflang 用新 helper `getAlternateLanguageMapFor` 只指 zh/ja/ko 三語，sitemap 同步、Footer 的 ja/ko 導向語系版。新增第一篇日文文章 `/ja/blog/pier2-accommodation`（駁二住宿決策文，zh/en/ja 三語互指；BLOG_TRANSLATIONS 型別擴充支援 ja，sitemap 翻譯條目 alternates 改含兄弟語言）。順手修 en 版 pier2 殘留的固定評分（4.5 星/75 則）與固定價格（12,000/8,000），補無電梯註記；ko.json 統一「고딘 하우스→거우딩 민박」。guide 頁非中文語系加「來信附日期人數館名」提示。驗證：lint、tsc、build:local、test:seo-page-health 全過；live 抽查 ja/ko title 單尾綴、FAQPage schema、四個新路由 200、llms-ko 內容、en 評分 0 殘留。Trip.com/攜程 listing 稽核（中國市場）：主站與商旅版皆顯示 5 房（實際 6）、有「Shared Dormitory 床位」錯誤房型、無包棟定位、僅 1 張照片、零評論——需在攜程後台改，屬 User 行動項。Naver 驗證等 User 註冊取碼。
- 2026-07-27 (第二批): 補齊 9-10 月排程空窗與 weekly-refresh 護欄並部署。新增兩篇排程文章：`kaohsiung-music-center-stay`（2026-09-13，高雄流行音樂中心演唱會住宿——90 天清單 6.2「高雄高流住宿」白名單首次落地，巨蛋文章零重疊）、`yancheng-walkable-stay`（2026-10-04，鹽埕步行圈住宿決策頁，導流 /godin）；兩篇皆 3 sections + 4 FAQ，數字與店名全部取自站內既定事實（高流步行約 15 分鐘、大港橋 8 分、捷運 5 分等）。`scripts/weekly-refresh.ts` 新增 `hasFrontmatterFaq` 護欄：frontmatter 已有 faq 的文章跳過每週區塊注入，防止已搬移的 AUTO-SEO-WEEKLY 區塊被重新附加造成重複顯示（護欄已用 kitchen-bnb=true / yancheng-snacks=false 實測）。驗證：`validate:content`（49+20 不變，新文章未來日期正確不計入）、`eslint` 通過；部署後 live 抽查首頁 200、未來文章正確 404、既有文章 200、sitemap 115 條不含未來文章。commit `1a9b328`。GSC 重新檢索已完成：以內建瀏覽器面板由 User 登入 GSC（hellohouse2017@gmail.com、資源 https://www.hello-stay.com/）後，5 條 URL（/、/kaohsiung-whole-house、/compare、/godin、/blog/kaohsiung-group-stay-guide）全部收到「已要求建立索引／已將網址加入優先檢索佇列」確認；舊 group-stay-guide 頁 GSC 已顯示「重新導向、未編入索引」，代表 Google 端已認知 308，SERP 殘留屬快取汰換中。技術備註：GSC 檢查列的 Enter 與「要求建立索引」按鈕對合成鍵盤/滑鼠事件無反應，需以 JS 原生 setter 設值＋dispatch KeyboardEvent＋element.click() 觸發；/inspect 深連結的 id 參數為不透明 token，無法自組。
- 2026-07-27: 完成「高雄包棟」SEO/AEO/GEO 全站稽核與優化並部署。錢頁分工重整：`/kaohsiung-whole-house` title/H1 改為商業型「高雄包棟民宿｜4-36 人整棟包棟・鹽埕駁二步行圈」吃主詞，`/compare` 改「高雄包棟民宿比較｜你好哇寓所vs溝頂民宿怎麼選」吃比較詞並補 BreadcrumbList，首頁 H1 補「民宿」成完整詞組，`/packages` 補 BreadcrumbList。`/godin` 新增 6 題可見 FAQ＋FAQPage/BreadcrumbList schema＋內鏈至 /kaohsiung-whole-house、/hellohouse、/compare、/book（90 天清單 5.3 首次落地）。GEO 事實修正：`.well-known/ai-plugin.json` 移除過期的 6-48 人/Google 4.9 星/溝頂 10-12/大智 2026 開幕，改為現行 4-36 口徑；`i18n/en|vi.json` 的 6–26 修為 8–26；vi/ko 的大智若愚補「規劃中未開放」；`llms-faq.ts` 新增包棟推薦/30 人雙館/電梯三題直答。4 篇 MDX（pier2-accommodation、kitchen-bnb、offsite-teambuilding、family-reunion）補 frontmatter faq 各 4 題（內容自內文忠實搬移，AUTO-SEO-WEEKLY 殘留區塊一併移除；family-reunion 的 6-36 修為 4-36）。修復內容閘門紅燈：排程文章 pier2-one-day-itinerary（publishDate 7/26 到期）與同名 MDX 撞號，排程版標 draft。sitemap 更新實改頁 lastModified 並降大智若愚 priority 0.8→0.5。驗證：`npm run lint`、`npm run validate:content`（49 排程＋20 MDX）、`npm run build:local`、`test:seo-page-health/route-boundaries/route-runtime/report-formatters/blog-title-lookup` 全過；production 部署 `hello-stay-next-4viytb2wc` 已 alias `https://www.hello-stay.com`，live 抽查新 title×3、godin FAQPage+BreadcrumbList+可見 FAQ、ai-plugin.json 新口徑、llms.txt 三新題、kitchen-bnb FAQPage、sitemap lastmod 2026-07-27 均正確。已知待辦：weekly-refresh 手動觸發會把已搬移區塊長回文末（workflow 排程已停用，僅手動有風險，已留獨立任務籤）；Google 舊快取（$10,000 起、6-48 人、已刪 group-stay-guide 舊頁）需在 GSC 對主要 URL 手動要求重新檢索加速汰換。
- 2026-07-23: 完成 LINE 對話上下文判定與公開事實同步修正。line-bot-v2 新增每回合 LLM TurnPlan，要求依目前訊息、最近回合、session 館別與候選知識共同還原 standaloneQuestion；知識層先依 LLM 選出的館別/設備事實回答，禁止 contextual plan 再退回單句關鍵字猜測，LLM 主備援皆不可用時僅允許 exact FAQ，否則通知人工。溝頂「有電梯嗎」現在會以結構化事實回答「溝頂民宿目前沒有電梯，館內動線需要走樓梯。」；新增 reply provenance、failover、contextual fact/clarification metrics 與告警。人工通知仍會送出，但案件通知後立即關閉並清除 activeCase，客人後續訊息繼續走自動流程，不進入等待人工回覆狀態。Booking Core 新增 public-facts API 與快取；官網比較表、文章與公開事實資料同步修正電梯資訊。驗證：line-bot-v2 `npm test` 179/179、`npm run check` 通過；Booking Core `npm run typecheck`、`npm run test:concierge-core` 73/73 與 production build 通過；主站 `npm run validate:content`、`npm run lint`、`npm run build:local` 通過。line-bot gateway 已部署至 `https://line-bot.hello-stay.com`（production active、主備 LLM 與 MongoDB health 通過），Booking Core 已部署含 `/api/integrations/concierge/public-facts` 至 `https://booking.hello-stay.com`（未簽名請求正確回 401），主站已部署至 `https://www.hello-stay.com` 並抽查電梯表與文章口徑。
- 2026-07-22: 依人工確認的客服口徑修正雙館、行李寄放與收據資訊。雙館改為兩館步行約 5 秒、標準 27-34 人且 35-36 人須加床；入住前行李限入住當天 12:00 後、須提前告知並一次寄放，寄放後不得留在現場等候；報帳僅提供可填抬頭與統編的合法收據，不開立統一發票或三聯式發票。同步更新首頁、FAQ、LLM 摘要、包棟頁與相關文章，移除退房後可寄放及三聯式發票的錯誤公開內容。
- 2026-07-20: 同步入住政策公開來源：延後退房統一為每小時 NT$1,800，超過 4 小時以續住一天計算；寵物經事前書面同意後每隻清潔費 NT$800。已更新主站入住須知、LLM FAQ、排程文章，並與 Booking Core 契約 PDF、電子簽署頁對齊；Booking Core 部署至 `https://booking.hello-stay.com`、主站部署至 `https://www.hello-stay.com`，正式頁面與 health endpoint 抽查通過。
- 2026-07-17: 完成並部署 30 天 SEO 與訂房轉換優化計畫。Booking Core production 為 `https://hello-stay-booking-next-svbwvzemx-hellohouses-projects.vercel.app`，正式網域 `https://booking.hello-stay.com`；主站 production 為 `https://hello-stay-next-lk3951rfr-hellohouses-projects.vercel.app`，已 alias 至 `https://www.hello-stay.com`。GA4 健檢已支援三參數 `gtag config` 並集中 Measurement ID；主站建立匿名 booking attribution、統一 `checkInDate/checkOutDate/property/guestCount`，Booking Core 擴充 `search_completed` / `payment_succeeded`、保存全漏斗歸因並提供 Bearer + `no-store` 的 `/api/internal/analytics/seo-funnel`。技術 SEO 改為 fast-xml-parser + cheerio 全 sitemap 爬檢，加入 `critical/warning/opportunity`、canonical/noindex/H1/OG/lang/hreflang/schema/FAQ/內鏈驗證，新 critical 與 recovery 可突破同日一次告警；robots 允許 `/_next/` 且保護 `/api`、`/admin`、`/go`，`/admin/social` 加 production server-side Basic Auth 與 noindex。GSC 改用 query clusters、URL 聯集、100 曝光 CTR 門檻、mixed 與 cannibalization；GA4 改報 7/28 天 Organic/AI/landing path，Booking funnel 與 Core Web Vitals 併入週報，技術/趨勢/內容分別採日/週/月節奏。內容面建立 `public-stay-facts.ts`，補齊你好哇 6 房（含 1301 雙人房），校正演唱會、三天兩夜、麻將、6 人與 20 人文章，合併舊演唱會 URL 為永久轉址，draft 不再進路由/sitemap/Related Articles；`/explore`、`/compare`、`/kaohsiung-whole-house` 也完成意圖與事實對齊。驗證：主站 `npm run lint`、`npm run build:local`（151 routes）、內容閘門通過；正式站 sitemap crawl 114 URL 為 0 critical / 0 warning，首頁與 Arena 200、舊演唱會 URL 308、GA4 script 存在、外語 html lang 正確、Social Admin 未授權 401。Booking Core `npm run verify` 通過（60 tests + production build），正式網域 health 200、未授權 funnel 401、授權 funnel 200。
- 2026-07-13: 將探索頁、美食與景點指南、鹽埕美食文章、全站文案收斂與 CSS token 清理版本部署至 Vercel production。Production deployment 為 `https://hello-stay-next-7drzcs05i-hellohouses-projects.vercel.app`，已 alias 至 `https://www.hello-stay.com`。`npm run lint`、`npm run build` 通過，Vercel 端完成 140 個頁面建置；正式網域首頁、`/explore`、`/explore/food`、`/explore/spots`、`/blog/yancheng-food-guide`、`/sitemap.xml` 均回 `200`，主要頁面內容 marker 已確認存在。
- 2026-07-12: 將首頁 UI/UX 與 SEO/AEO/GEO 一致性改善部署至 Vercel production。執行 `vercel deploy --prod --yes` 後，production deployment 為 `https://hello-stay-next-h11pjckl8-hellohouses-projects.vercel.app`，並已 alias 到 `https://www.hello-stay.com`。上線前 `npm run lint`、`npm run build:local`（137/137 靜態頁）、`npm run test:seo-page-health`、`npm run test:seo-route-runtime`、`npm run test:seo-route-boundaries`、`npm run test:seo-report-formatters` 均通過；正式網域首頁、`/llms.txt`、`/robots.txt`、`/sitemap.xml` 均回 `200`，首頁 canonical / OG / Twitter / hreflang、Organization / WebSite / WebPage / 兩個 LodgingBusiness / Service / ItemList / FAQPage JSON-LD、6 題可見 FAQ 均存在。實測 30 人推薦雙館並帶入 `guestCount=30`，36 人以上提供 LINE 人工確認，輪播暫停功能可正常切換，桌機無水平捲動且只有一個 `<main>`。
- 2026-07-12: 完成首頁 UI/UX 與 SEO/AEO/GEO 一致性改善。`HomeTemplateExperience` 新增 4-36 人的 30 秒方案比較、合法登記/步行距離/入住時間信任資訊、6 題可見 FAQ 與日期/人數防呆；輪播加入暫停控制，館別卡圖片改為 lazy loading，並移除巢狀 `<main>`。手機版改為先顯示真實住宿照片，主標不再孤字換行，導覽只保留可訂館別與高意圖入口，且移除互相重疊的兩個浮動聯絡按鈕，改成首屏內可點的 LINE 客服。`src/app/page.tsx` 補回首頁 page-level hreflang、OG/Twitter 圖片，JSON-LD 重建為 Organization / WebSite / WebPage / 兩個 LodgingBusiness / Service / ItemList / FAQPage 的單一圖譜，移除不適用的 SearchAction 與 speakable，避免同一 `@id` 對應不同實體。同步把實際訂房系統雙館上限 36 人回寫到首頁、llms 多語摘要、文章與內容資料，移除 llms 中未即時驗證的固定評價/旅客數/價格範例；另以實際圖片檢查修正首頁與兩館 gallery 的錯誤圖片標籤，移除把人物形象照、節目照與市場照誤標成房間/外觀的情況。驗證：`npm run lint`、`npm run build:local`（137/137 靜態頁）、`npm run test:seo-page-health`、`npm run test:seo-route-runtime`、`npm run test:seo-route-boundaries`、`npm run test:seo-report-formatters` 通過；production preview `http://127.0.0.1:3011/` 以桌機 1440×1000 與手機 390×844 驗證無水平溢出、僅一個 `<main>`、6 題 FAQ、首頁 `og:image` / hreflang / JSON-LD 類型正確，且 30 人導向雙館、40 人改導 LINE 人工確認。
- 2026-07-07: 將 2026-07-06 完成的 AEO / GEO / SEO 優化版本部署至 Vercel production。執行 `vercel deploy --prod --yes` 後，production deployment 為 `https://hello-stay-next-i65b27jsl-hellohouses-projects.vercel.app`，並已 alias 到 `https://www.hello-stay.com`。線上驗證：正式網域首頁、`/llms.txt`、`/blog/taiwan-travel-foreign-guide`、`/blog/kaohsiung-mahjong-stay` 皆回 `200`；`/llms.txt` 已含動態文章連結，`/blog/kaohsiung-mahjong-stay` HTML 已含 FAQPage schema。Vercel 原生 deployment URL 受團隊 SSO 保護會跳轉驗證頁，不影響正式網域流量。
- 2026-07-06: 依 `AEO_SEO_GEO_PLAN.md` 完成 M1-M3 主體，先不做 M4 翻譯。`src/lib/articles.ts` 與 `src/app/blog/[slug]/page.tsx` 現在支援 MDX frontmatter `faq`、輸出可見 FAQ 區塊與 FAQPage / Article 補強欄位（`articleSection`、`wordCount`）；同步把 `kaohsiung-mahjong-stay.mdx`、`kaohsiung-group-trip.mdx` 的既有問答轉成 frontmatter，並在 `taiwan-travel-foreign-guide.mdx` 留下待補 FAQ 的 MDX TODO。`src/app/llms*.txt/route.ts` 改為由 `src/lib/llms.ts`、`src/data/llms-faq.ts` 與 `src/data/properties.ts` / 文章清單動態生成；`src/app/layout.tsx` 與 `src/lib/ai-assistant-referrers.ts` 新增 AI referrer allowlist，將 ChatGPT / Perplexity / Gemini / Claude / Copilot 等來源標記為 `ai-assistant`；新增 `docs/geo-tracking.md` 說明量測方式。另優化首頁與 `/godin` metadata CTR 文案，並讓 `scripts/seo-driven-update.ts` 在無 `MONGODB_URI` 時優雅輸出空清單，且輸出 `currentTitle`、`currentDescription`、`suggestedAction`。驗證：`npx eslint`（本次修改檔案）通過；`npm run test:seo-page-health`、`npm run test:seo-route-runtime`、`npm run test:seo-route-boundaries`、`npm run test:seo-report-formatters`、`npm run test:seo-blog-title-lookup`、`npx tsx scripts/test-seo-ga4.ts`、`npm run seo:analyze` 通過；`npm run lint` 與 `npm run build` 通過。
- 2026-06-20: 依使用者要求移除 `src/app/hellohouse/page.tsx` 只出現在主館頁面的 `Answer Block / 這一頁先給你答案` 區塊，避免單一館別被額外突顯而破壞三館一致性。驗證：`rg` 已無該字串殘留，`npx eslint src/app/hellohouse/page.tsx` 通過。
- 2026-06-20: 針對使用者指出的殘留字串做最後清理，補修 `src/components/MoneyPageTemplate.tsx` 的 `LINE 立即詢問` CTA、`src/data/scheduled-articles.ts` 的 `押金確認無損壞後匯款退還`，並把 `/book` 入口調整為更接近文件的「先選館別再查空房與報價」口徑，連同 `src/app/book/page.tsx` 與 `src/app/book/BookingFlow.tsx` 的標題、說明與 schema 一次對齊。驗證：全站 `rg` 已無 `LINE 立即詢問` / `押金` / `訂金` / `尾款` 等殘留命中；`npx eslint src/components/MoneyPageTemplate.tsx src/data/scheduled-articles.ts src/app/book/page.tsx src/app/book/BookingFlow.tsx` 通過，`npm run build:local` 成功。
- 2026-06-20: 依 `deep-research-report.md` 再做一輪公開頁面文案收斂，將首頁、比較、方案、入住須知、交通、周邊探索、評價、關於我們、三館頁與訂房橋接頁的英文字 kicker、`LINE 詢問` 與內部流程語換成對外旅客口徑，並把 `豪宅級中島廚房` 統一修成 `豪華中島廚房`。同步修正 `src/app/llms*.txt/route.ts`、`src/data/properties.ts`、`src/i18n/zh.json` 與相關共用元件，確保首頁與 AI 摘要資料層一致。驗證：`npx eslint` targeted check 通過，`npm run build:local` 成功；production deploy `https://hello-stay-next-5bnwlzrfq-hellohouses-projects.vercel.app` 已 alias 至 `https://www.hello-stay.com`，並抽查首頁、`/compare`、`/agreement` 已回應新文案。
- 2026-06-20: 追加一輪 production residue cleanup 並重新上線。清理 `src/data/properties.ts`、`src/app/llms*.txt/route.ts`、`src/components/ProposalCard.tsx`、`src/app/blog/pier2-accommodation/page.tsx`、`src/app/blog/kaohsiung-offsite-teambuilding/page.tsx`、`src/app/kaohsiung-whole-house/page.tsx`、`src/content/articles/kaohsiung-offsite-teambuilding.mdx`、`src/content/articles/kaohsiung-group-trip.mdx`、`src/data/scheduled-articles.ts` 與 `src/i18n/*` / `src/components/i18n/*` 中殘留的舊口徑，重點移除 `10-12 / 6-12 / 6-48 / 平日$10,000起 / 即將開幕` 等舊描述，統一成溝頂 `4-12`、目前可訂 `4-26`、大智若愚 `規劃中，尚未開放訂房`。驗證：`npm run build:local` 通過；production 部署至 `https://hello-stay-next-jiumdexa5-hellohouses-projects.vercel.app`，alias 成功切到 `https://www.hello-stay.com`；正式站首頁、`/blog/kaohsiung-offsite-teambuilding`、`/llms.txt`、`/llms-full.txt` 已回應新字串。
- 2026-06-20: 針對第一次上線後仍可見的殘留舊口徑再做一輪 production cleanup。將 `src/components/Navbar.tsx` 的提示列 `官網選館別` 改為 `官網看方案`；修正 `src/app/page.tsx` 首頁 JSON-LD 內的 `6-48` 舊總量敘述與溝頂 `10-12` 舊描述，改成目前可訂 `4-26` 與溝頂 `4-12` 的口徑；同步更新 `src/app/llms.txt/route.ts`、`src/app/llms-full.txt/route.ts`，把大智若愚統一成 `規劃中，尚未開放訂房`，並把溝頂容量改成 `4-12 人整棟包棟`。驗證：`npx eslint` targeted check 通過，`npm run build:local` 通過；production 先後部署至 `https://hello-stay-next-h7cgi9ffb-hellohouses-projects.vercel.app` 與最終 `https://hello-stay-next-r33iul9tz-hellohouses-projects.vercel.app`，皆 alias 至 `https://www.hello-stay.com`，且正式站首頁與 `llms*.txt` 已回應新字串。
- 2026-06-20: 將最新文案對齊版本部署至 Vercel production。以本機 `npm run build:local` 再次確認 Next.js production build 通過後，執行 `vercel deploy --prod --yes`，production deployment 為 `https://hello-stay-next-g7v1j08lf-hellohouses-projects.vercel.app`，並成功 alias 到 `https://www.hello-stay.com`。線上驗證：`https://hello-stay.com` 會 `301` 導向 `https://www.hello-stay.com/`，且正式站首頁、`/book`、`/agreement`、`/compare`、`/dazhi` 已回應本輪新文案與狀態口徑。
- 2026-06-20: 依使用者最新公開頁面回饋，完成一輪全站文案對齊與禁用詞清查。重寫 `src/app/agreement/page.tsx` 為全額預收制版本，移除 `押金` / `訂金` / `尾款` 語境；收斂 `src/app/compare/page.tsx` 與 `src/app/kaohsiung-whole-house/page.tsx` 的 Dazhi 狀態，統一改成「尚未開放訂房」且不再與可訂館別並列；同步把首頁與訂房頁的 `館別` 系統詞換成 `方案 / 房型 / 選項`，並修正 `src/components/PropertyLinksBlock.tsx`、`src/components/PropertyDetailTemplate.tsx` 的規劃中標籤。另清理 `src/i18n/*` 的 `6-48` 標題、三篇公開文章 frontmatter、`src/data/scheduled-articles.ts` 的 `訂金（通常 50%）` 與 `押金確認後退還`、以及 `packages / about / reviews / blog / traffic` 等次要頁面的內部簡報式標題。驗證：`npx eslint` 針對修改檔案僅出現 `mdx/json` ignored 警告、無錯誤；`npm run build:local` 成功完成。
- 2026-05-23: 依使用者回饋修正新版首頁卡片互動一致性：`src/components/HomeTemplateExperience.tsx` 的館別卡片圖片與「房型說明」按鈕不再導往舊館別頁，而是在同一個 `hello-stay` 模板風格內開啟房型說明 modal；內容直接取用 `src/data/properties.ts` 既有真實房型資料，僅展示房型照片、樓層、房名、坪數/人數、床型與精簡說明，不帶入舊頁長內容或其他 CTA。大智若愚維持規劃中口徑，只顯示一層三房一廳、可包層/包棟與 2027 年中正式公告為準。同步修正首頁日期 filter 使用本機日期，避免台灣時區早上被 UTC 轉成前一天。驗證：`npm run lint`、`npm run build:local` 通過；production preview 3002 以 Playwright 驗證桌機與手機點擊房型說明皆不跳頁、modal 圖片載入完成、無水平 overflow，首頁 canonical / GSC verification / JSON-LD 維持存在。
- 2026-05-23: 依使用者要求先建立 `hello-stay-template-redesign-plan.md`，明確列出模板對齊範圍、SEO/GSC/真實訂房不可破壞邊界、真實館別資料映射與四階段 checklist；同步建立 thread heartbeat automation `hello-stay-template-redesign-heartbeat` 以 30 分鐘節奏回報計劃進度。完成首頁重新套用 `/hello-stay` 模板方向：新增 `src/components/HomeTemplateExperience.tsx` 作為首頁 client component，重現 `Luxury Stays` hero、segmented search mode、日期/人數/館別/房數 filter panel、三張橫向 villa listing cards、Journal / FAQ 與 final CTA；`src/app/page.tsx` 改為保留 metadata 與 LodgingBusiness / Organization / FAQPage / WebSite JSON-LD 後接新 component，移除舊 cinematic 首頁渲染；`src/app/globals.css` 補上 `.hs-*` 模板首頁樣式，`src/components/Navbar.tsx` 的 alert bar 文案改為更接近模板 luxury direct booking 語氣。未修改 `.env` 或 `next.config.ts`，未引入 fake localStorage booking、假 AI chat、Unsplash 假圖或假價格。驗證：`npm run lint`、`npm run build:local`、`npm run test:seo-route-boundaries`、`npm run test:seo-route-runtime`、`npm run test:seo-page-health` 全部通過；production preview `http://127.0.0.1:3002/` 以 Playwright 驗證首頁 canonical、GSC verification、JSON-LD 類型、robots.txt、sitemap hreflang、`/book` canonical/JSON-LD 均存在，桌機與手機截圖皆無水平 overflow。
- 2026-05-22: 依使用者要求在「保留目前線上 SEO / GSC / 真實訂房功能」前提下，將既有站點大幅重塑為 `hello-stay` 模板風格，而非重新引入 Gemini 版本的假資料與假預訂。更新 `src/app/globals.css` 全域設計系統為暖奶油底、深棕灰文字、銅色主 CTA、模板式卡片與搜尋面板；重寫 `src/components/Navbar.tsx` 為含官方直訂提示列、三館/比較/方案/交通/預訂導覽與多語系選單的全站 shell；重寫 `src/components/Footer.tsx` 為模板式資訊架構；改造 `src/app/book/BookingFlow.tsx` 為新版搜尋面板但仍沿用 `NEXT_PUBLIC_BNB_API` 空房查詢與 LINE 預填訊息；重塑 `src/components/ProposalCard.tsx` 為同風格提案卡。未修改 `.env`、`next.config.ts`、GSC verification、canonical、robots / sitemap 設定。驗證：`npm run lint`、`npm run build:local`、`npm run test:seo-route-boundaries`、`npm run test:seo-route-runtime`、`npm run test:seo-page-health` 通過；本機 production preview 以 3002 檢查首頁與 `/book` canonical / GSC meta / JSON-LD、`robots.txt` 完整 crawler 規則、`sitemap.xml` hreflang 開頭，並用 headless Chromium 驗證桌機與手機版無水平溢出。
- 2026-05-22: 依上線版首頁作為基準，撤回 Gemini 套入的整包新版首頁與假功能：移除 `src/components/new-template/*`、`src/data/new-data.ts`、`src/types/new-types.ts`、`src/components/LayoutWrapper.tsx`，並還原 `src/app/page.tsx`、`src/app/layout.tsx`、`src/app/globals.css`、`next.config.ts`、`package.json`、`package-lock.json` 到既有架構，避免假 localStorage 預訂、缺失 `/api/chat` 的 AI 管家、Unsplash 假圖與多語系首頁 wrapper 退化進入正式站。基於原首頁新增「官方直訂入口」rail，保留真實可爬內鏈至 `/book`、`/compare`、`/packages`、`/traffic` 與 LINE；同步將 `public/robots.txt` 補齊為線上 Vercel 同等完整 crawler/disallow 規則，避免 public 靜態檔壓過 `src/app/robots.ts` 後輸出簡化版。`npm run lint`、`npm run build:local`、`npm run test:seo-route-boundaries`、`npm run test:seo-page-health` 均通過；本機 production server 驗證 `/robots.txt` 完整、首頁回 200、JSON-LD 存在、核心內鏈與新直訂入口文字正確輸出。
- 2026-05-22: 完成 `hello-stay` 新模板套入既有 `Hellostay官網` 的上線前稽核。結論：目前不建議部署新版首頁；雖然 `npm run build:local` 可通過、SEO route/page-health 測試可通過，但 `npm run lint` 仍有新模板 React hooks / unescaped text 錯誤，且本機 `/robots.txt` 實際輸出被 `public/robots.txt` 壓成簡化版，與線上 Vercel 完整 crawler/disallow 規則不一致。另確認新版首頁以 client-side modal / localStorage 模擬預訂成功、AI 管家呼叫不存在的 `/api/chat`、首頁 crawlable 內鏈較線上版大幅減少，並使用 Unsplash 假圖與多處未對齊真實館別/價格/開幕狀態的文案資料；需先修復後才能評估上線。
- 2026-05-22: 完成階段 3.3 AIEO HowTo schema 補強：`src/app/book/page.tsx` 在既有 WebPage / ReserveAction / FAQPage JSON-LD 外，新增「如何預訂 Hello Stay 高雄包棟民宿」HowTo，包含輸入日期、填寫人數、查詢空房、透過 LINE 確認報價四步；`src/app/traffic/page.tsx` 新增「如何搭捷運與高鐵到 Hello Stay 高雄包棟民宿」HowTo，對應高鐵左營站、捷運紅線、美麗島轉橘線、鹽埕埔站步行抵達四步。`npx eslint src/app/book/page.tsx src/app/traffic/page.tsx` 通過，`npm run build:local` 通過。
- 2026-05-22: 延續 SEO/AIEO 重複意圖收斂檢查，確認 `/packages` 與 `/kaohsiung-whole-house` 分工仍可保留：前者承接企業團建、婚禮迎娶、家族旅遊、球隊移訓等活動情境，後者承接高雄包棟總覽、人數級距與三館比較。同步修正兩頁的一致性問題：`/kaohsiung-whole-house` title 年份由 2025 改為 2026、溝頂民宿價格口徑統一為平日約 `$8,000` 起、大智若愚價格改為「即將公布」、比較表修正溝頂廚房與手動麻將資訊；`/packages` 移除「客廳 / 大客廳」誤稱，改為「一樓公共空間 / 挑高公共空間」。另外修復已被首頁引用的 `src/components/new-template/JournalSection.tsx` 亂碼語法錯誤，重建為可編譯的文章入口與 AI Overview FAQ 區塊。`npx eslint` targeted check 通過，`npm run build:local` 通過。
- 2026-05-21: 成功於系統安裝 Antigravity CLI 終端工具，二進位檔路徑為 `/Users/tangyukao/.local/bin/agy`，並完成 Shell 設定檔 (.zshrc、.zprofile 等) 的環境變數 PATH 設定與驗證。
- 2026-05-20: 執行高轉化率 SEO 文章擴充與國際搜尋引擎收錄計畫。1. 在 `src/data/scheduled-articles.ts` 中手動撰寫並植入 6 篇與現有 32 篇文章完全無重疊的高意圖 SEO 攻略文，包含：「高雄演唱會包棟」、「大港開唱後勤」、「港澳星馬包車指南」、「高雄跨年包棟」、「結婚迎娶包棟」以及「學生畢旅/班遊包棟」，精準打擊高含金量搜尋；2. 在 `src/app/layout.tsx` 的 metadata 區塊新增 `naver-site-verification` 與 `y_key` 標籤，並綁定環境變數，以利日韓兩大搜尋引擎（Naver / Yahoo Japan）收錄網站；3. 同步產出 4 篇帶有 UTM 追蹤連結的論壇/社群導流文案，可直接應用於 Dcard、Threads 與 FB 旅遊社團，完善流量攔截與追蹤機制。
- 2026-05-20: 全面清查並修復專案中剩餘的 ESLint 警告，包含移除 `src/app/godin/page.tsx`、`src/components/MoneyPageTemplate.tsx` 與 `src/components/ProposalCard.tsx` 中未使用的變數及引入；修正 `src/components/RoomGallery.tsx` 中未使用表達式（Unused expression）的錯誤；在 `src/app/admin/social/page.tsx` 中加入停用 `no-img-element` 的規則以容許外部圖片載入；同時清理 `scripts/` 目錄下多個 `.mjs` 和 `.js` 腳本中未使用變數與 `require` 相關的警告，達成 `npm run lint` 零錯誤與零警告的完美健康狀態。
- 2026-05-20: 成功透過實體 Chrome 瀏覽器自動化技術，繞過 Google 登入安全限制，將新撰寫的台灣旅遊攻略長文 (/blog/taiwan-travel-foreign-guide) 提交至 Google Search Console (GSC) 並順利觸發「要求建立索引」，成功將網址加入優先檢索佇列，加速搜索引擎及 AI 引擎的抓取與收錄。
- 2026-05-20: 清查並修復全站核心前端元件的 ESLint / TypeScript 型別錯誤。包含修正 Navbar 中 useEffect 內的 setState 警告、ChatWidget 中的 unescaped 符號及 gtag 轉型（並清理未使用的 useEffect 引入）、LineFloatingCTA 中的 gtag safe casting、以及 GodinPageContent/HelloHousePageContent 中 room/floor 的 as any 型別安全轉型；清空所有 src 下的 ESLint 語法錯誤，使 npm run build:local 生產環境打包與 TypeScript 型別檢查 100% 成功通過。
- 2026-05-20: 針對國際旅客來台旅遊與包棟痛點，撰寫並上架專屬攻略長文 (/blog/taiwan-travel-foreign-guide)。深入剖析外國人開車自由行、台灣 110V/Type A 電壓插座、入住前後行李安全寄存、全聯 PX Mart 採買便利性、以及免安裝 LINE 的官方 Email 多語系客服等五大旅遊痛點；文末佈局高轉化搜尋字詞，串聯三館比較、包棟方案、查房等交易頁面，達成完美的流量轉化閉環。本機打包 `npm run build:local` 通過且 TypeScript 型別檢測順利。
- 2026-05-20: 實作國際旅客全站入口 Email 與 LINE 雙通道多語系適配優化。1. 改造右下角懸浮 `ChatWidget` 元件，利用 `usePathname` 動態偵測當前頁面語系，為外語（英、日、韓、越）旅客自動翻譯訂房助手文案並提供「Email 聯絡 + LINE 聯絡」雙按鈕，其中 Email mailto 連結為第一優先，且為四國語系貼心預填了精美的「主旨與訂房資訊格式內容」，降低外國人預訂阻礙；2. 改造 `LineFloatingCTA` 綠色懸浮 LINE 按鈕，設定在外語版（非中文）下自動隱藏，避免對沒有 LINE 或不懂中文的外國旅客造成視覺干擾，把右下角聯絡焦點完全保留給支援多語系與 Email 管道的 ChatWidget；3. 本機型別檢測與靜態建置打包 100% 順利通過。
- 2026-05-20: 實作國際旅客專屬高雄觀光與包棟指南 (/guide) 路由與元件。1. 新建 `GuidePageContent.tsx` 共用元件，融合 Reveal 動態特效與多語系 alternates SEO 宣告，完整導覽包棟私密優勢、捷運交通、駁二與鹽埕景點美食，並提供外國旅客專屬 FAQ，且特別新增 Email 諮詢雙聯絡通道卡片 (mailto 連結)，排除外國旅客沒有 LINE 的溝通阻礙；2. 建立五個對應靜態路由（`/guide`、`/ja/guide`、`/ko/guide`、`/en/guide`、`/vi/guide`），並擴充五國語系檔 `guide` 翻譯與 FAQ Email 回答節點（同步補齊 `zh.json` 防止 TS型別編譯報錯）；3. 修正 Footer 連結邏輯，非中文語系下將原本連往不存在 `/locale/blog` 的 404 連結優雅導向 `/guide`，中文語系則保留 `/blog`；4. 更新 `sitemap.ts` 納入此核心靜態路徑。本機打包 `npm run build:local` 順利通過，TypeScript 型別檢測 100% 合規，122 個靜態網頁 prerender 順利。
- 2026-05-20: 執行麻將設備文案修正與日韓語系 SEO/AIEO 詮釋資料最佳化。1. 修正全站（含 TSX 與 MDX 檔案）所有關於麻將之誤導文案（如「免洗牌」、「卡牌」等電動麻將相關字詞），改為精準呈現「手動麻將桌」與「傳統手動麻將」，並轉化為溫馨的洗牌聚會意圖；2. 深度優化日語（ja.json）與韓語（ko.json）語系檔，精準佈局「一棟貸し」（整棟包棟）與「독채 펜션」（獨棟別墅）等日韓超高點擊率核心關鍵字，將麻將譯文改為「手動麻雀卓 / 수동 마작 테이블」，完成實體資訊對齊。本機打包 `npm run build:local` 通過且 TypeScript 型別檢測順利。
- 2026-05-20: 執行 SEO/AIEO 重建優化第四階段（圖片語意豐富化與空間屬性修正）。1. 重構全站所有民宿大樓外觀、公共空間與房間圖片之 alt 標籤，融入多模態 AI 檢索所需的富語意及核心搜尋字詞（如「高雄包棟民宿」、「鹽埕民宿」等）；2. 依據使用者回饋針對「你好哇寓所沒有客廳，只有中島廚房」進行修正，徹底清查並修正全站中外文圖片 alt 標籤、好評及 FAQ 中所有誤導性的「客廳」或「living room」描述，統一改為強調「一樓挑高公共空間、豪華中島廚房、高腳椅吧台與交誼聚會大空間」；3. 在 `HelloHousePageContent.tsx` 與 `GodinPageContent.tsx` 的房型與樓層資料結構中新增 `imgAlt` 欄位並完成動態渲染綁定，確保多語系（英、日、韓、越）的 alt 皆能正確渲染在地化富語意。本機編譯打包 `npm run build:local` 與 TypeScript 型別檢測順利通過。
- 2026-05-20: 執行 SEO/AIEO 重建優化第三階段（語意地圖橫向內鏈重構）。手動梳理並修改專案下所有 8 篇靜態攻略 MDX 文章（家族旅遊、團體行程、廚房自煮、麻將配備、跨年包棟、企業移地訓練、婚禮迎娶、駁二住宿），在段落中自然嵌入彼此的主題連結以提升站內橫向導流，並在文末添加「讀者下一個決策問題」之推薦區塊，取代原本生硬的機械式列表。本機編譯打包 `npm run build:local` 順利通過。
- 2026-05-20: 執行 SEO/AIEO 重建優化第二階段（品牌實體對齊）。1. 新建 `src/app/about/page.tsx` 品牌起源故事與旗下合法民宿認證頁面；2. 在首頁及關於我們頁面的 JSON-LD 中，引入 `parentOrganization` 與 `subOrganization` 的樹狀實體階層宣告，對齊 GMB Google 商家實體（你好哇寓所與溝頂民宿）；3. 更新全語系 `footer` 翻譯與 `src/components/Footer.tsx` 關於我們 Link 連結；4. 更新 `src/app/sitemap.ts` 納入 `/about` 靜態路徑。本機編譯打包 `npm run build:local` 通過。
- 2026-05-20: 執行 SEO/AIEO 重建優化首要階段。1. 將 `.github/workflows` 下的四個工作流（`daily-update`、`weekly-refresh`、`weekly-redeploy`、`monthly-enrich`）之 `schedule` 移除，從源頭停用機械式更新以避免商品化模板文案的寫入，保留 `workflow_dispatch` 作為手動觸發入口；2. 手動清理 `kaohsiung-kitchen-bnb.mdx` 中重複的手寫「預訂前快速判斷」區塊，保留標籤內的版本以維持單一渲染，排除 Duplicate Content 的隱患；3. 在 `/godin` 頁面的 `LodgingBusiness` schema 補上對應 GMB 搜尋之 `sameAs` 實體綁定以加強品牌權威。本機建置 `npm run build:local` 順利通過。
- 2026-05-20: 針對 5/19 SEO 健康日報回報的警告與 CTR 問題進行代碼優化。1. 修正 `/godin` 與 `/dazhi` 的 Meta Description 過短警告，補足為包含人數、核心特色與地理優勢的高轉化文案；2. 針對核心字「高雄包棟民宿」（排名第 8 但 0 點擊）與「高雄包棟推薦」（排名第 1 但 0 點擊）進行首頁 `src/app/page.tsx` 的 Meta Title 與 Description 重構。在標題首部引入「【包棟$8,000起】」與「官方直訂免手續費」以提高搜尋結果頁（SERP）上的視覺吸引力與點擊率（CTR）。`npx eslint` targeted check 與本機編譯打包 `npm run build:local` 皆順利通過。
- 2026-05-20: 研讀 Threads 專家 `@darkseoking` 關於 AIO/SEO 資訊增益與語意地圖的貼文，並產出詳細的 `seo_aieo_audit_report.md` 審計報告。報告指出了網站目前五大 SEO/AIEO 隱患：1. 「自動新鮮度腳本」造成的重複內容問題（例如 `kaohsiung-kitchen-bnb.mdx` 中因 weekly-refresh 插入而產生的重複 H2 與套模板套話）；2. 品牌名稱與 Google 商家實體脫鉤且缺乏 About 頁面；3. 文章間橫向內鏈貧乏，未形成語意地圖；4. 文末 CTA 太過跳躍，未接住使用者下一個問題；5. 圖片 Alt Text 太過單薄不利多模態 AI 檢索。報告同時針對這些問題給出了具體的重建優化方向。
- 2026-05-20: 完成「同關鍵字雙入口」第一步收斂：將與交易主頁高度重疊的 `/blog/kaohsiung-group-stay-guide` 刪除，改由 `src/data/pruned-blog-slugs.ts` 透過 `/blog/[slug]` 統一永久 redirect 到 `/kaohsiung-whole-house`；同步刪除對應的 `src/app/blog/kaohsiung-group-stay-guide/page.tsx` 與 `src/content/articles/kaohsiung-group-stay-guide.mdx`，並把既有薄頁 redirect 連鎖一起理順，讓 `kaohsiung-graduation-trip -> /kaohsiung-whole-house`、`kaohsiung-sports-team -> /packages` 直接命中新承接頁，不再先繞到 group-stay-guide。站內所有舊攻略推薦位一併改成直接指向 `/kaohsiung-whole-house`、`/compare` 或其他仍保留的差異化文章，`RelatedArticles` 與 `/blog` 列表敘述也同步清理。`npx eslint` targeted check 通過（僅 `src/app/godin/page.tsx` 仍有既有 2 個 unused warnings），`npm run build:local` 通過，`curl -I` 以 webpack dev server 驗證 `/blog/kaohsiung-group-stay-guide -> /kaohsiung-whole-house`、`/blog/kaohsiung-graduation-trip -> /kaohsiung-whole-house`、`/blog/kaohsiung-sports-team -> /packages`，且目標頁皆為 `200`
- 2026-05-20: 完成 `1.1 砍薄頁 + redirect` 第五步（功能頁）：將 `/features/kitchen`、`/features/parking` 兩個薄型功能頁刪除，改由 `src/app/features/[slug]/page.tsx` 搭配 `src/data/pruned-feature-slugs.ts` 統一永久 redirect 到更強承接頁：`kitchen -> /blog/kaohsiung-kitchen-bnb#kitchen`、`parking -> /traffic#parking`；同步把 `src/app/traffic/page.tsx` 停車區塊補上 `#parking` 錨點，並將 `src/app/kaohsiung-whole-house/page.tsx`、`src/app/compare/page.tsx`、`src/app/packages/page.tsx` 與 `src/app/blog/kaohsiung-kitchen-bnb/page.tsx` 的站內舊功能頁連結改成直接指向新落點；sitemap 移除 feature 子頁條目。`npx eslint` targeted check 通過，`npm run build:local` 通過，`curl -I` 以 webpack dev server 驗證兩條舊 feature URL 全部回 `308` 到正確頁面且目標頁皆 `200`
- 2026-05-19: 依「熱門內容頁幫首頁拉權重」策略補強站內內部連結：新增 `src/components/HomepageIntentBlock.tsx` 作為首頁權重導向 CTA 區塊，並插入 `src/app/explore/page.tsx`、`src/app/blog/kaohsiung-group-stay-guide/page.tsx`、`src/app/blog/kaohsiung-kitchen-bnb/page.tsx`、`src/app/blog/kaohsiung-mahjong-stay/page.tsx` 的前段 / 中段；同時更新 `src/app/blog/[slug]/page.tsx`，讓動態攻略頁也在文末先出現回首頁 / 比較頁 / 查房頁的關鍵字導流，再接 `PropertyLinksBlock` 與相關文章。重點錨文字聚焦在「高雄包棟民宿推薦」「高雄包棟推薦」「高雄包棟民宿首頁」等，不再只用 generic CTA。`npx eslint` targeted check 通過，`npm run build:local` 通過
- 2026-05-19: 依 GSC「有曝光、沒點擊」問題再做一輪 CTR 導向調整，重點不是價格一致性本身，而是搜尋結果頁的標題與落地頁意圖是否值得點擊：更新 `src/app/page.tsx` 首頁 metadata、hero eyebrow / H1 / 首屏賣點，明確對齊「高雄包棟民宿推薦」；更新 `src/app/compare/page.tsx` 的 metadata 與 H1，改聚焦「高雄包棟推薦怎麼選」；更新 `src/app/packages/page.tsx` 的 metadata 與 H1，改承接「高雄團體住宿 / 家族旅遊包棟」；更新 `src/app/blog/kaohsiung-group-stay-guide/page.tsx` 的 metadata、H1 與 FAQ，讓攻略頁更像會被點的比較型內容；更新 `src/app/godin/page.tsx` 的 metadata、FAQ 與 hero 副標，補強「鹽埕民宿」與「10-12 人小包棟」意圖承接。`npx eslint` targeted check 通過（僅 `src/app/godin/page.tsx` 仍有既有 2 個 unused warnings），`npm run build:local` 通過
- 2026-05-19: 清理 `src/data/scheduled-articles.ts` 內與現況不符的舊價格文案，移除排程文章裡殘留的 `平日 $10,000 起 / 10000 元起 / 一萬元起` 敘述；針對溝頂民宿與 10-12 人小團體情境統一改成平日約 `$8,000` 起，針對 `你好哇寓所` 與混合館別文章改寫為「依人數安排報價」或「依館別與人數報價」，避免和目前館別頁、比較頁、方案頁的價格口徑衝突；同步修正 6 人住宿文章中把溝頂民宿寫成可正式開伙的描述，改回較保守的公共空間 / 宵夜採買說法。`rg` 確認舊價字串已清空，`npx eslint src/data/scheduled-articles.ts` 通過，`npm run build:local` 通過
- 2026-05-19: 補強 `/packages`、`/reviews` 兩個轉單與信任頁：`src/app/packages/page.tsx` 改聚焦「高雄包棟民宿方案推薦」，補上更明確的活動情境分流、首頁／比較頁／查空房導流、可見 FAQ，並修正方案頁對溝頂民宿的價格口徑為平日約 `$8,000` 起，避免和 compare / 館別頁衝突；`src/app/reviews/page.tsx` 改聚焦「高雄包棟民宿評價」，補上主館 Google 評價僅對應你好哇寓所的可見說明、FAQ 與比較／訂房導流，同步修正溝頂民宿評論文案中與實際設備不符的「製冰機 / 煮飯 / 全聯 3 分鐘」敘述，避免評論頁與館別頁資訊打架。`npx eslint` targeted check 通過，`npm run build:local` 通過，Playwright 本機驗證 `/packages`、`/reviews` 的 title、首屏關鍵字、CTA 與評價說明皆正確渲染
- 2026-05-19: 補強 `/features/kitchen`、`/features/parking` 兩個設備意圖頁並修正內容一致性：`src/app/features/kitchen/page.tsx` 改聚焦「高雄有廚房的包棟民宿推薦」，補上 openGraph、首頁／方案頁／查空房導流，並修正溝頂民宿廚房資訊為「流理台＋雙門冰箱＋微波爐、不可正式開伙」，避免與 `/godin`、`/compare` 的既有描述衝突；`src/app/features/parking/page.tsx` 改聚焦「高雄包棟民宿停車指南」，補上 FAQ 與交易頁 CTA，並將停車場清單改為與 `/traffic` 同源；新增 `src/data/parking-lots.ts` 作為共用停車資料，`src/app/traffic/page.tsx` 同步改讀此檔，避免後續再出現停車資訊分歧。`npx eslint` targeted check 通過，`npm run build:local` 通過，Playwright 本機驗證 `/features/kitchen`、`/features/parking` 的 title、CTA 與繁體文案皆正確渲染
- 2026-05-19: 延續同批 GSC CTR / 排名優化，補強 `/compare`、`/traffic`、`/book` 三個交易承接頁：`src/app/compare/page.tsx` 的 metadata 改聚焦「高雄包棟民宿推薦比較」，首屏新增回首頁／包棟方案／查空房導流；`src/app/traffic/page.tsx` 改聚焦「高雄包棟民宿交通停車指南」，補上鹽埕埔站與停車意圖文案、回首頁與方案頁內鏈；`src/app/book/page.tsx` 與 `src/app/book/BookingFlow.tsx` 改成「高雄包棟民宿空房查詢與報價」承接，新增 canonical、FAQ/ReserveAction schema 與官方直訂導流區塊。`npx eslint` targeted check 通過，`npm run build:local` 通過，Playwright 本機驗證 `/compare`、`/traffic`、`/book` 的 title 與首屏關鍵字 / CTA 已正確渲染
- 2026-05-19: 依 2026-05-17 的 GSC CTR / 排名觀察完成首頁與內容頁的 SEO 點擊率優化：首頁 `src/app/page.tsx` 的 `<title>` / description 改聚焦「高雄包棟民宿」「可開伙麻將」「近鹽埕美食」，hero 首屏補上可見關鍵詞，並在 LodgingBusiness schema 新增 `amenityFeature`；`src/components/PropertyLinksBlock.tsx` 升級成帶有首頁／方案頁關鍵字導流的共用 CTA，`src/app/blog/[slug]/page.tsx` 一併改用該區塊；`src/app/explore/page.tsx` 同步改成「鹽埕美食地圖」導向並加入回首頁／查空房 CTA，以承接先前從 `/blog/yancheng-food-guide` 轉往 `/explore` 的搜尋意圖。`npx eslint` targeted check 通過，`npm run build:local` 通過，Playwright 本機驗證 `/`、`/explore`、`/blog/kaohsiung-group-stay-guide` title 與 CTA 已正確渲染
- 2026-05-19: 完成 `1.1 砍薄頁 + redirect` 第四步（人數頁）：將 `/capacity/10`、`/capacity/20`、`/capacity/30`、`/capacity/40` 四個模板化薄頁刪除，改由 `src/app/capacity/[slug]/page.tsx` 搭配 `src/data/pruned-capacity-slugs.ts` 統一永久 redirect 到更強承接頁：`10 -> /godin`、`20 -> /hellohouse`、`30 -> /kaohsiung-whole-house#capacity-30`、`40 -> /dazhi`；同步把 `src/app/kaohsiung-whole-house/page.tsx` 的「依人數找包棟」擴成可承接搜尋意圖的容量卡片、補強 `src/app/hellohouse/page.tsx` 的 20 人包棟 FAQ/schema，並將 compare 與站內 `PropertyLinksBlock` 等舊容量內鏈直接改指向新落點；sitemap 與 IndexNow 靜態清單移除 capacity 子頁。`npx eslint` targeted check 通過，`npm run build:local` 通過，`curl -I` 以 webpack dev server 驗證四條舊 capacity URL 全部回 `308` 到正確頁面且目標頁皆 `200`；另外確認 Next 16 Turbopack 在中文 workspace 路徑下對新 `/capacity/[slug]` route 會 panic，屬開發伺服器工具鏈問題，非 route 邏輯錯誤
- 2026-05-19: 完成 `1.1 砍薄頁 + redirect` 第三步（場景頁）：將 `/occasion/family-trip`、`/occasion/friends-gathering`、`/occasion/company-retreat` 三個薄型場景頁刪除，改由 `src/app/occasion/[slug]/page.tsx` 搭配 `src/data/pruned-occasion-slugs.ts` 統一永久 redirect 到 `/kaohsiung-whole-house` 的對應場景錨點；同步把 `src/app/kaohsiung-whole-house/page.tsx` 的「依需求找包棟」區塊擴成可承接搜尋意圖的場景卡片，並將首頁與 compare 頁的相關內鏈直接改指向新錨點，sitemap 移除 occasion 子頁條目，IndexNow 靜態頁清單補上 `/kaohsiung-whole-house` 與 `/compare` 主入口。`npx eslint` targeted check 通過，`npm run build:local` 通過，`curl -I` 本機驗證三條舊 occasion URL 全部回 `308` 到對應錨點
- 2026-05-19: 完成 `1.1 砍薄頁 + redirect` 第二步：將 `/compare/hellohouse-vs-godin`、`/compare/hellohouse-vs-dazhi`、`/compare/godin-vs-dazhi` 三個薄型比較子頁刪除，改由新的 `src/app/compare/[slug]/page.tsx` 搭配 `src/data/pruned-compare-slugs.ts` 統一永久 redirect 回 `/compare` 主比較頁；同步移除 compare 子頁的 sitemap 條目，並把 `/compare` 內原本導向子頁的「詳細比較」區塊改成直接導向三館介紹頁，讓比較意圖只保留單一主入口。`npx eslint` targeted check 通過，`npm run build:local` 通過，`curl -I` 本機驗證三條舊 compare URL 全部回 `308` 到 `/compare`
- 2026-05-19: 完成 `1.1 砍薄頁 + redirect` 第一步：將 `kaohsiung-graduation-trip`、`kaohsiung-sports-team`、`kaohsiung-bnb-recommendation`、`yancheng-food-guide` 四篇薄頁從 `src/content/articles` 與對應 `app/blog/*` 靜態頁移除，改由 `/blog/[slug]` 依 `src/data/pruned-blog-slugs.ts` 統一做永久 redirect（Next.js 實際回 `308`）：前兩者導向 `/blog/kaohsiung-group-stay-guide`，`kaohsiung-bnb-recommendation` 導向 `/`，`yancheng-food-guide` 導向 `/explore`；同步把 blog 列表、sitemap、相關文章與站內內鏈清掉舊 slug，避免被重新收錄或從站內再次導回。`npm run build:local` 通過，`curl -I` 本機驗證四條 redirect 全部命中；本次 targeted eslint 僅剩 `src/app/godin/page.tsx` 既有兩個 unused warning
- 2026-05-19: 完成 `0.4 GMB / Google Maps 一致性` 修正：人工核對主館「你好哇・寓所民宿 (Hello House B&B)」目前 Google 評分為 `4.5 / 75 則評論`，因此將全站多館頁與總覽頁上的 Google 評分文案統一改為明確標示「主館你好哇寓所」，移除首頁、`/reviews`、`/capacity/*` 等非單館頁的 `aggregateRating` schema，並刪除溝頂民宿缺乏獨立 Google 商家頁支撐的評分暗示；同步清掉 `4.8` / `4.9` 舊文案殘留與未來排程文章中的過期分數。`npm run build:local` 通過；`npm run lint` 仍受專案既有 scripts/components 規則錯誤影響，與本次修改無直接關聯
- 2026-05-19: 完成第一輪 SEO/AIEO 技術修正：移除全站 `keywords` meta、補上 `googleBot` snippet/image/video preview robots 設定、將 `sitemap.xml` 改為靜態頁固定 `lastModified` 與 blog 內容日期、清理首頁品牌 schema 的 `alternateName` 混淆、統一大智若愚為「預計 2027 年中開幕」、並把 `/blog` 列表改為直接讀取 `src/content/articles` MDX frontmatter 作為單一來源；`npm run lint` 與 `npm run build:local` 驗證通過
- 2026-05-15: 確認 Vercel 警示信來源；`hello-stay.com` apex domain 在 Vercel 專案 `hello-stay-next` 內被判定為 misconfigured，原因是目前 nameserver 指向 Cloudflare，且 apex A 記錄未設為 Vercel 要求的 `76.76.21.21`；`www.hello-stay.com` 仍正常指向 Vercel
- 2026-04-20: 修正 GSC 索引「重新導向錯誤」— 在 next.config.ts 加入 /en/, /ja/, /ko/, /vi/ 的明確 301 redirect 規則，以及多語系子頁面的預防性 trailing slash 規則
- 2026-04-30: 新增本機不進版控的 `.local/` 敏感資料目錄，暫存台灣旅宿網操作用帳號資訊；未修改 `.env`，以符合專案工作規則
